# Set the numlock to be on. -> Because it makes sense...
numlockx &

# Bind keys. 
xmodmap ~/.config/.xmodmap &
xbindkeys

# Monitors. 
./.config/autostart/monitorsetup.sh

# Wallpaper restore
nitrogen --restore

# Start picom
picom -b 

[[ -f ~/.profile ]] && . ~/.profile
